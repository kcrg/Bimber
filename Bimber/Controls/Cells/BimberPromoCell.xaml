<DataTemplate x:Class="Bimber.Controls.Cells.BimberPromoCell" x:DataType="models:BimberPromoModel"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:Bimber.Models">
    <Grid ColumnDefinitions="20*,80*" RowDefinitions="25*,75*">
        <Label
            HorizontalOptions="Center"
            VerticalOptions="Center"
            Text="{Binding Glyph}"
            TextColor="{Binding PromoColor}" />

        <Label
            Grid.RowSpan="2"
            Grid.ColumnSpan="2"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            Text="{Binding Title}" />

        <Label
            Grid.Row="1"
            Grid.ColumnSpan="2"
            HorizontalOptions="Center"
            VerticalOptions="Start"
            Text="{Binding Description}" />
    </Grid>

</DataTemplate>
<ContentView x:Class="Bimber.Views.CarouselPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:scv="clr-namespace:MLToolkit.Forms.SwipeCardView;assembly=MLToolkit.Forms.SwipeCardView"
    BackgroundColor="White">
    <Grid>
        <scv:SwipeCardView VerticalOptions="FillAndExpand" LoopCards="True">
            <scv:SwipeCardView.ItemTemplate>
                <DataTemplate>
                    <Frame>
                        <Label
                            VerticalTextAlignment="Center"
                            BackgroundColor="Beige"
                            FontSize="Large"
                            HorizontalTextAlignment="Center"
                            Text="{Binding .}" />
                    </Frame>
                </DataTemplate>
            </scv:SwipeCardView.ItemTemplate>
            <scv:SwipeCardView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>Card 1</x:String>
                    <x:String>Card 2</x:String>
                    <x:String>Card 3</x:String>
                </x:Array>
            </scv:SwipeCardView.ItemsSource>
        </scv:SwipeCardView>
    </Grid>
</ContentView>